---
interface Props {
  items: string[]
}

const { items } = Astro.props
---
<div class="bg-gray-50 border-l-4 border-blue-700 p-4 rounded-r-lg my-6">
  <p class="text-xs font-semibold uppercase tracking-wide text-blue-700">Quick Answer</p>
  <ul class="mt-2 space-y-1 text-gray-700">
    {items.map((item) => (
      <li class="flex gap-2">
        <span class="text-blue-700 font-bold" aria-hidden="true">·</span>
        <span>{item}</span>
      </li>
    ))}
  </ul>
</div>
